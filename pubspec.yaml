name: flutter_long_link_debug_panel
description: Flutter long connection debug panel.
version: 1.0.0
repository: https://github.com/AlistairLi/flutter_long_link_debug_panel

environment:
  sdk: '>=2.19.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
